name: "ArgoCD Actions"
description: "Operate your ArgoCD applications from GitHub"
author: "omegion"
inputs:
  address:
    description: "ArgoCD server address"
    required: true
  token:
    description: "ArgoCD token"
    required: true
  action:
    description: "Action name"
    required: true
  appName:
    description: "Application name to sync"
    required: true

runs:
  using: "composite"
  steps:
    - uses: docker://ghcr.io/omegion/argocd-actions:v0.1.0-5-g7d9af84
    - id: random-number-generator
      run: |
        ls
        env
      shell: sh
#runs:
#  using: "docker"
#  image: "docker://ghcr.io/omegion/argocd-actions:v0.1.0-5-g7d9af84"
#  args:
#    - 'action'
#    - ${{ inputs.action }}
#    - "--application=${{ inputs.appName }}"
#    - "--token=${{ inputs.token }}"
#    - "--address=${{ inputs.address }}"
#    - "--logLevel=debug"
