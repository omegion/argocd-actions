name: Local

on:
  push:
#    branches-ignore:
#      - '**'

jobs:
  local_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: ./
      - name: Self test
        id: selftest
        uses: ./@i-11
        with:
          address: argocd.omegion.dev
          token: 1
          action: sync
          appName: blog


